# Nginx example:
```html
systemctl restart nginx
systemctl status nginx
```
# Troubleshooting:
```html
nginx -t                                                                                                                ### check config
nginx -T
```